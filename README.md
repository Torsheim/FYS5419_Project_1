# FYS5419 Project 1 report package

This package contains an updated `report/project1_report.tex` and `report/project1_report.pdf`.
The report has no AI declaration section. The reproducibility/code organization section is formatted as a two-column appendix with terminal commands and a repository overview.

The figures needed by the LaTeX source are included in `results/figures/`.
